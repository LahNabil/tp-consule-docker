server:
  port: 8081

spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery: SERVICE-CLIENT
  application:
    name: SERVICE-CLIENT
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Micro_lachclientservicedb?createDatabaseIfNotExist=true
    username: "root"
    password: "BTlfo9malmo9atila10#"
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        
